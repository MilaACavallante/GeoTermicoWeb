<?php
require_once 'vendor/autoload.php';

//$mergedObject = getTimersAndDataBJSON();

//loadDBInicial();

//salvaDataB(getTimersAndDataBJSON());

//$document = getAllDocuments();


//echo 'ok';

//salvaDataB($mergedObject);

//$mergedObject = (object) array_merge((array) $data[0], (array) $timers[0]);

//

/*
$filter = $data[1]; 
$update = ['$push' => ['0.DATAB' => $data[0]->DATAB]];

// Call the updateDocument function with the filter and update parameters
$result = updateDocument($filter, $update);

if ($result) {
    echo "Document updated successfully.";
} else {
    echo "Failed to update document.";
}*/

?>